% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backcalc_corrs.R
\name{backcalc_corrs}
\alias{backcalc_corrs}
\title{Backcalc Missing Inferential Statistics for Correlations}
\usage{
backcalc_corrs(
  r = NULL,
  se = NULL,
  n = NULL,
  df = NULL,
  p = NULL,
  ci = NULL,
  one_sided = FALSE,
  digits = 3
)
}
\arguments{
\item{r}{Numeric. Correlation coefficient. Can be a single value or a vector of length 2 (for group comparisons).}

\item{se}{Numeric. Standard error of the Fisher z-transformed correlation.}

\item{n}{Numeric. Sample size. For two-sample cases, provide a vector of length 2.}

\item{df}{Numeric. Degrees of freedom (optional). If not provided, will be inferred from \code{n}.}

\item{p}{Numeric. p-value (optional). If not provided, it will be inferred.}

\item{ci}{Numeric vector of length 2. Confidence interval for the correlation (on raw scale, not Fisher z).}

\item{one_sided}{Logical. Whether the test is one-sided (default is FALSE).}

\item{digits}{Integer. Number of significant digits for rounding results (default = 3).}
}
\value{
A named numeric vector with the following elements:
\describe{
\item{Estimnate}{Estimated correlation or difference in correlations.}
\item{SE}{Standard error of the Fisher z-transformed correlation.}
\item{t or z}{Test statistic (depending on whether df is available).}
\item{df}{Degrees of freedom (if available or inferred).}
\item{p or p-one}{Two-sided or one-sided p-value.}
\item{LL}{Lower bound of the confidence interval (on correlation scale).}
\item{UL}{Upper bound of the confidence interval (on correlation scale).}
}
}
\description{
\code{backcalc_corrs()} reconstructs inferential statistics for correlation coefficients
using Fisher's z-transform. It supports reconstructing standard errors, confidence intervals,
p-values, and test statistics from partial or varied combinations of summary and inferential statistics.
}
\examples{
# One-sample: r + SE + n (df inferred)
backcalc_corrs(r = 0.45, se = 0.1, n = 25, digits = 3)

# One-sample: r + p-value + df provided (t-test)
backcalc_corrs(r = 0.52, p = 0.02, df = 18, digits = 3)

# One-sample: r + confidence interval + df
backcalc_corrs(r = 0.35, ci = c(0.10, 0.55), df = 20, digits = 3)

# Two-sample: Provide two rs, equal n, infer difference & df
backcalc_corrs(r = c(0.60, 0.40), n = c(30, 30), digits = 3)

# Two-sample: Provide two rs with unequal ns, infer df
backcalc_corrs(r = c(0.70, 0.50), n = c(40, 25), digits = 3)

# Two-sample: r difference + p-value + df (t-test)
backcalc_corrs(r = 0.18, p = 0.03, df = 45, digits = 3)

}
